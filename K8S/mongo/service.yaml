apiVersion: v1
kind: Service
metadata:
  namespace: three-tier
  name: mongo-service
spec:
  selector:
    app: mongo
  ports:
  - name: mongo-service
    protocol: TCP
    port: 27017
    targetPort: 27017